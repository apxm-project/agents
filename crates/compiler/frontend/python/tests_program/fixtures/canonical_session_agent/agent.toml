capabilities = []
display_name = "Canonical Session Agent"
domain = "general"
id = "canonical-session-agent"
kind = "agent"
license = "MIT"
schema_version = "apxm.agent.v1"
version = "0.1.0"

[compile]
entry = "python/session.py"
frontend = "python"

[runtime]
memory_space = "stm"
session_prefix = "canonical-session"

[source]
type = "local"
