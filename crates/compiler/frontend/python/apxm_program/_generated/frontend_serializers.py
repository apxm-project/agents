# AUTO-GENERATED by apxm codegen frontend-serializers; DO NOT EDIT.
"""One serializer per apxm.frontend-graph contract record.

Each function holds every required key the contract states, then each optional
key whose value is stated, in the contract's own key order. A key the contract
adds, drops, or makes optional moves here on the next regeneration rather than
needing an edit in two languages.

A serializer reads either a generated record from `frontend_records` or the
equivalent bound-tree record, which carries the same field names — the bound
tree still spells a value expression and a predicate literal as the plain
mapping the contract states, so `_field` reads a mapping key as readily as an
attribute.

The emitters' two policy derivations — placing a yielded resume value in its
enclosing region's block, and annotating a loop's first body region — are not
contract projections and stay hand-written in `_emit`.
"""

from __future__ import annotations

from collections.abc import Mapping
from typing import Any, Union

from .frontend_graph import (
    PREDICATE_COMPARATOR_EQUALS,
    PREDICATE_COMPARATOR_NOT_EQUALS,
    PREDICATE_COMPARATOR_TRUTHY,
    PREDICATE_SCALAR_TYPE_BOOLEAN,
    PREDICATE_SCALAR_TYPE_INTEGER,
    PREDICATE_SCALAR_TYPE_NULL,
    PREDICATE_SCALAR_TYPE_STRING,
    PROGRAM_AUTHORING_KIND_AGENT,
    PROGRAM_AUTHORING_KIND_WORKFLOW,
    SKILL_INSTRUCTION_KIND_ENTRY,
    SKILL_INSTRUCTION_KIND_INLINE,
    VALUE_EXPRESSION_KIND_ARRAY,
    VALUE_EXPRESSION_KIND_BOOLEAN,
    VALUE_EXPRESSION_KIND_CONTEXT,
    VALUE_EXPRESSION_KIND_INTEGER,
    VALUE_EXPRESSION_KIND_NULL,
    VALUE_EXPRESSION_KIND_OBJECT,
    VALUE_EXPRESSION_KIND_PROJECTION,
    VALUE_EXPRESSION_KIND_SSA,
    VALUE_EXPRESSION_KIND_STRING,
)

from .frontend_records import (
    AgentAuthoring,
    ArrayExpression,
    Block,
    BooleanExpression,
    BooleanLiteral,
    CallIntent,
    CapabilityRequirement,
    ContextEdge,
    ContextExpression,
    ControlIntent,
    ControlPredicate,
    DataEdge,
    Declaration,
    EntrypointInputSchema,
    EqualsPredicate,
    EventRequirement,
    FunctionDef,
    HookBinding,
    ImportedProgramRef,
    IntegerExpression,
    IntegerLiteral,
    ModelRequirement,
    NotEqualsPredicate,
    NullExpression,
    NullLiteral,
    ObjectExpression,
    Parameter,
    PredicateLiteral,
    ProgramAuthoring,
    ProgramDefinition,
    ProjectionExpression,
    Region,
    SkillEntrySource,
    SkillInlineSource,
    SkillInstructionSource,
    SkillRequirement,
    SsaExpression,
    StringExpression,
    StringLiteral,
    TruthyPredicate,
    Value,
    ValueExpression,
    ValueField,
    WorkflowAuthoring,
)

from .permissions import Permission, PermissionDecision


def _field(record: Any, name: str) -> Any:
    """Read one contract field from a generated record or its wire mapping."""
    if isinstance(record, Mapping):
        return record.get(name)
    return getattr(record, name, None)


def serialize_permission(record: Permission) -> Union[PermissionDecision, dict[str, Any]]:
    """One `PermissionDecision`: the bare decision, or the object form when it gives a reason."""
    decision = _field(record, "decision")
    reason = _field(record, "reason")
    return decision if reason is None else {"decision": decision, "reason": reason}


def serialize_program_definition(record: ProgramDefinition) -> dict[str, Any]:
    """One `ProgramDefinition` in contract key order: every required key, then each stated optional key."""
    emitted: dict[str, Any] = {
        "program_id": _field(record, "program_id"),
        "entrypoint": _field(record, "entrypoint"),
        "input_type_ref": _field(record, "input_type_ref"),
        "output_type_ref": _field(record, "output_type_ref"),
        "has_default_context": _field(record, "has_default_context"),
    }
    authoring = _field(record, "authoring")
    if authoring is not None:
        emitted["authoring"] = serialize_program_authoring(authoring)
    context_type_ref = _field(record, "context_type_ref")
    if context_type_ref is not None:
        emitted["context_type_ref"] = context_type_ref
    default_context = _field(record, "default_context")
    if default_context is not None:
        emitted["default_context"] = serialize_value_expression(default_context)
    input_contract = _field(record, "input_contract")
    if input_contract is not None:
        emitted["input_contract"] = input_contract
    input_schema = _field(record, "input_schema")
    if input_schema is not None:
        emitted["input_schema"] = serialize_entrypoint_input_schema(input_schema)
    return emitted


def serialize_workflow_authoring(record: WorkflowAuthoring) -> dict[str, Any]:
    """One `WorkflowAuthoring` in contract key order: every required key, then each stated optional key."""
    emitted: dict[str, Any] = {
        "kind": _field(record, "kind"),
    }
    return emitted


def serialize_agent_authoring(record: AgentAuthoring) -> dict[str, Any]:
    """One `AgentAuthoring` in contract key order: every required key, then each stated optional key."""
    emitted: dict[str, Any] = {
        "kind": _field(record, "kind"),
        "primary_model_ref": _field(record, "primary_model_ref"),
    }
    return emitted


def serialize_entrypoint_input_schema(record: EntrypointInputSchema) -> dict[str, Any]:
    """One `EntrypointInputSchema` in contract key order: every required key, then each stated optional key."""
    emitted: dict[str, Any] = {
        "type": _field(record, "type"),
    }
    additionalProperties = _field(record, "additionalProperties")
    if additionalProperties is not None:
        emitted["additionalProperties"] = additionalProperties
    items = _field(record, "items")
    if items is not None:
        emitted["items"] = serialize_entrypoint_input_schema(items)
    properties = _field(record, "properties")
    if properties is not None:
        emitted["properties"] = {key: serialize_entrypoint_input_schema(item) for key, item in properties.items()}
    required = _field(record, "required")
    if required is not None:
        emitted["required"] = list(required)
    return emitted


def serialize_event_requirement(record: EventRequirement) -> dict[str, Any]:
    """One `EventRequirement` in contract key order: every required key, then each stated optional key."""
    emitted: dict[str, Any] = {
        "node_id": _field(record, "node_id"),
        "type_id": _field(record, "type_id"),
        "payload_schema": serialize_entrypoint_input_schema(_field(record, "payload_schema")),
        "schema_digest": _field(record, "schema_digest"),
    }
    return emitted


def serialize_imported_program_ref(record: ImportedProgramRef) -> dict[str, Any]:
    """One `ImportedProgramRef` in contract key order: every required key, then each stated optional key."""
    emitted: dict[str, Any] = {
        "program_ref": _field(record, "program_ref"),
        "artifact_digest": _field(record, "artifact_digest"),
        "entrypoint": _field(record, "entrypoint"),
        "target_agent_identity_requirement": _field(record, "target_agent_identity_requirement"),
    }
    return emitted


def serialize_declaration(record: Declaration) -> dict[str, Any]:
    """One `Declaration` in contract key order: every required key, then each stated optional key."""
    emitted: dict[str, Any] = {
        "decl_id": _field(record, "decl_id"),
        "decl_kind": _field(record, "decl_kind"),
        "input_type_ref": _field(record, "input_type_ref"),
        "output_type_ref": _field(record, "output_type_ref"),
    }
    context_default_present = _field(record, "context_default_present")
    if context_default_present is not None:
        emitted["context_default_present"] = context_default_present
    payload_schema = _field(record, "payload_schema")
    if payload_schema is not None:
        emitted["payload_schema"] = serialize_entrypoint_input_schema(payload_schema)
    target_ref = _field(record, "target_ref")
    if target_ref is not None:
        emitted["target_ref"] = target_ref
    return emitted


def serialize_function_def(record: FunctionDef) -> dict[str, Any]:
    """One `FunctionDef` in contract key order: every required key, then each stated optional key."""
    emitted: dict[str, Any] = {
        "function_id": _field(record, "function_id"),
        "parameters": [serialize_parameter(item) for item in _field(record, "parameters")],
        "body_region_id": _field(record, "body_region_id"),
        "is_entrypoint": _field(record, "is_entrypoint"),
    }
    result_type_ref = _field(record, "result_type_ref")
    if result_type_ref is not None:
        emitted["result_type_ref"] = result_type_ref
    return emitted


def serialize_parameter(record: Parameter) -> dict[str, Any]:
    """One `Parameter` in contract key order: every required key, then each stated optional key."""
    emitted: dict[str, Any] = {
        "value_id": _field(record, "value_id"),
        "type_ref": _field(record, "type_ref"),
        "role": _field(record, "role"),
    }
    return emitted


def serialize_value(record: Value) -> dict[str, Any]:
    """One `Value` in contract key order: every required key, then each stated optional key."""
    emitted: dict[str, Any] = {
        "value_id": _field(record, "value_id"),
        "type_ref": _field(record, "type_ref"),
        "origin": _field(record, "origin"),
    }
    expression = _field(record, "expression")
    if expression is not None:
        emitted["expression"] = serialize_value_expression(expression)
    origin_id = _field(record, "origin_id")
    if origin_id is not None:
        emitted["origin_id"] = origin_id
    return emitted


def serialize_ssa_expression(record: SsaExpression) -> dict[str, Any]:
    """One `SsaExpression` in contract key order: every required key, then each stated optional key."""
    emitted: dict[str, Any] = {
        "kind": _field(record, "kind"),
        "value_id": _field(record, "value_id"),
    }
    return emitted


def serialize_context_expression(record: ContextExpression) -> dict[str, Any]:
    """One `ContextExpression` in contract key order: every required key, then each stated optional key."""
    emitted: dict[str, Any] = {
        "kind": _field(record, "kind"),
        "property_path": list(_field(record, "property_path")),
    }
    return emitted


def serialize_projection_expression(record: ProjectionExpression) -> dict[str, Any]:
    """One `ProjectionExpression` in contract key order: every required key, then each stated optional key."""
    emitted: dict[str, Any] = {
        "kind": _field(record, "kind"),
        "root": serialize_value_expression(_field(record, "root")),
        "property_path": list(_field(record, "property_path")),
    }
    return emitted


def serialize_object_expression(record: ObjectExpression) -> dict[str, Any]:
    """One `ObjectExpression` in contract key order: every required key, then each stated optional key."""
    emitted: dict[str, Any] = {
        "kind": _field(record, "kind"),
        "fields": [serialize_value_field(item) for item in _field(record, "fields")],
    }
    return emitted


def serialize_value_field(record: ValueField) -> dict[str, Any]:
    """One `ValueField` in contract key order: every required key, then each stated optional key."""
    emitted: dict[str, Any] = {
        "name": _field(record, "name"),
        "value": serialize_value_expression(_field(record, "value")),
    }
    return emitted


def serialize_array_expression(record: ArrayExpression) -> dict[str, Any]:
    """One `ArrayExpression` in contract key order: every required key, then each stated optional key."""
    emitted: dict[str, Any] = {
        "kind": _field(record, "kind"),
        "items": [serialize_value_expression(item) for item in _field(record, "items")],
    }
    return emitted


def serialize_string_expression(record: StringExpression) -> dict[str, Any]:
    """One `StringExpression` in contract key order: every required key, then each stated optional key."""
    emitted: dict[str, Any] = {
        "kind": _field(record, "kind"),
        "value": _field(record, "value"),
    }
    return emitted


def serialize_integer_expression(record: IntegerExpression) -> dict[str, Any]:
    """One `IntegerExpression` in contract key order: every required key, then each stated optional key."""
    emitted: dict[str, Any] = {
        "kind": _field(record, "kind"),
        "value": _field(record, "value"),
    }
    return emitted


def serialize_boolean_expression(record: BooleanExpression) -> dict[str, Any]:
    """One `BooleanExpression` in contract key order: every required key, then each stated optional key."""
    emitted: dict[str, Any] = {
        "kind": _field(record, "kind"),
        "value": _field(record, "value"),
    }
    return emitted


def serialize_null_expression(record: NullExpression) -> dict[str, Any]:
    """One `NullExpression` in contract key order: every required key, then each stated optional key."""
    emitted: dict[str, Any] = {
        "kind": _field(record, "kind"),
    }
    return emitted


def serialize_block(record: Block) -> dict[str, Any]:
    """One `Block` in contract key order: every required key, then each stated optional key."""
    emitted: dict[str, Any] = {
        "block_id": _field(record, "block_id"),
        "region_id": _field(record, "region_id"),
        "block_arguments": list(_field(record, "block_arguments")),
        "execution_order": _field(record, "execution_order"),
    }
    return emitted


def serialize_region(record: Region) -> dict[str, Any]:
    """One `Region` in contract key order: every required key, then each stated optional key."""
    emitted: dict[str, Any] = {
        "region_id": _field(record, "region_id"),
        "region_role": _field(record, "region_role"),
        "execution_order": _field(record, "execution_order"),
    }
    parent_region_id = _field(record, "parent_region_id")
    if parent_region_id is not None:
        emitted["parent_region_id"] = parent_region_id
    return emitted


def serialize_data_edge(record: DataEdge) -> dict[str, Any]:
    """One `DataEdge` in contract key order: every required key, then each stated optional key."""
    emitted: dict[str, Any] = {
        "from_value": _field(record, "from_value"),
        "to_consumer": _field(record, "to_consumer"),
        "consumer_slot": _field(record, "consumer_slot"),
    }
    return emitted


def serialize_call_intent(record: CallIntent) -> dict[str, Any]:
    """One `CallIntent` in contract key order: every required key, then each stated optional key."""
    emitted: dict[str, Any] = {
        "node_id": _field(record, "node_id"),
        "intent_kind": _field(record, "intent_kind"),
        "parent_region_id": _field(record, "parent_region_id"),
        "execution_order": _field(record, "execution_order"),
    }
    binding_ref = _field(record, "binding_ref")
    if binding_ref is not None:
        emitted["binding_ref"] = binding_ref
    operand_values = _field(record, "operand_values")
    if operand_values is not None:
        emitted["operand_values"] = list(operand_values)
    receiver_kind = _field(record, "receiver_kind")
    if receiver_kind is not None:
        emitted["receiver_kind"] = receiver_kind
    result_value = _field(record, "result_value")
    if result_value is not None:
        emitted["result_value"] = result_value
    return emitted


def serialize_control_intent(record: ControlIntent) -> dict[str, Any]:
    """One `ControlIntent` in contract key order: every required key, then each stated optional key."""
    emitted: dict[str, Any] = {
        "node_id": _field(record, "node_id"),
        "control_kind": _field(record, "control_kind"),
        "parent_region_id": _field(record, "parent_region_id"),
        "execution_order": _field(record, "execution_order"),
    }
    body_region_ids = _field(record, "body_region_ids")
    if body_region_ids is not None:
        emitted["body_region_ids"] = list(body_region_ids)
    operand_values = _field(record, "operand_values")
    if operand_values is not None:
        emitted["operand_values"] = list(operand_values)
    predicate = _field(record, "predicate")
    if predicate is not None:
        emitted["predicate"] = serialize_control_predicate(predicate)
    result_value = _field(record, "result_value")
    if result_value is not None:
        emitted["result_value"] = result_value
    return emitted


def serialize_truthy_predicate(record: TruthyPredicate) -> dict[str, Any]:
    """One `TruthyPredicate` in contract key order: every required key, then each stated optional key."""
    emitted: dict[str, Any] = {
        "root_value_id": _field(record, "root_value_id"),
        "property_path": list(_field(record, "property_path")),
        "comparator": _field(record, "comparator"),
    }
    return emitted


def serialize_equals_predicate(record: EqualsPredicate) -> dict[str, Any]:
    """One `EqualsPredicate` in contract key order: every required key, then each stated optional key."""
    emitted: dict[str, Any] = {
        "root_value_id": _field(record, "root_value_id"),
        "property_path": list(_field(record, "property_path")),
        "comparator": _field(record, "comparator"),
        "literal": serialize_predicate_literal(_field(record, "literal")),
    }
    return emitted


def serialize_not_equals_predicate(record: NotEqualsPredicate) -> dict[str, Any]:
    """One `NotEqualsPredicate` in contract key order: every required key, then each stated optional key."""
    emitted: dict[str, Any] = {
        "root_value_id": _field(record, "root_value_id"),
        "property_path": list(_field(record, "property_path")),
        "comparator": _field(record, "comparator"),
        "literal": serialize_predicate_literal(_field(record, "literal")),
    }
    return emitted


def serialize_context_edge(record: ContextEdge) -> dict[str, Any]:
    """One `ContextEdge` in contract key order: every required key, then each stated optional key."""
    emitted: dict[str, Any] = {
        "from_node": _field(record, "from_node"),
        "to_node": _field(record, "to_node"),
        "context_type_ref": _field(record, "context_type_ref"),
        "value_id": _field(record, "value_id"),
    }
    return emitted


def serialize_hook_binding(record: HookBinding) -> dict[str, Any]:
    """One `HookBinding` in contract key order: every required key, then each stated optional key."""
    emitted: dict[str, Any] = {
        "hook_id": _field(record, "hook_id"),
        "scope": _field(record, "scope"),
        "phase": _field(record, "phase"),
        "target_selector": _field(record, "target_selector"),
        "declaration_order": _field(record, "declaration_order"),
        "handler_ref": _field(record, "handler_ref"),
        "handler_digest": _field(record, "handler_digest"),
        "input_type_ref": _field(record, "input_type_ref"),
        "output_type_ref": _field(record, "output_type_ref"),
        "return_mode": _field(record, "return_mode"),
        "body_region_id": _field(record, "body_region_id"),
    }
    assigned_context_value_id = _field(record, "assigned_context_value_id")
    if assigned_context_value_id is not None:
        emitted["assigned_context_value_id"] = assigned_context_value_id
    return emitted


def serialize_capability_requirement(record: CapabilityRequirement) -> dict[str, Any]:
    """One `CapabilityRequirement` in contract key order: every required key, then each stated optional key."""
    emitted: dict[str, Any] = {
        "capability_ref": _field(record, "capability_ref"),
    }
    requested_permission = _field(record, "requested_permission")
    if requested_permission is not None:
        emitted["requested_permission"] = serialize_permission(requested_permission)
    tool_schema_present = _field(record, "tool_schema_present")
    if tool_schema_present is not None:
        emitted["tool_schema_present"] = tool_schema_present
    return emitted


def serialize_model_requirement(record: ModelRequirement) -> dict[str, Any]:
    """One `ModelRequirement` in contract key order: every required key, then each stated optional key."""
    emitted: dict[str, Any] = {
        "model_target_ref": _field(record, "model_target_ref"),
    }
    return emitted


def serialize_skill_requirement(record: SkillRequirement) -> dict[str, Any]:
    """One `SkillRequirement` in contract key order: every required key, then each stated optional key."""
    emitted: dict[str, Any] = {
        "skill_id": _field(record, "skill_id"),
        "instruction_source": serialize_skill_instruction_source(_field(record, "instruction_source")),
    }
    return emitted


def serialize_skill_entry_source(record: SkillEntrySource) -> dict[str, Any]:
    """One `SkillEntrySource` in contract key order: every required key, then each stated optional key."""
    emitted: dict[str, Any] = {
        "kind": _field(record, "kind"),
        "path": _field(record, "path"),
    }
    return emitted


def serialize_skill_inline_source(record: SkillInlineSource) -> dict[str, Any]:
    """One `SkillInlineSource` in contract key order: every required key, then each stated optional key."""
    emitted: dict[str, Any] = {
        "kind": _field(record, "kind"),
        "text": _field(record, "text"),
    }
    return emitted


def serialize_boolean_literal(record: BooleanLiteral) -> dict[str, Any]:
    """One `BooleanLiteral` in contract key order: every required key, then each stated optional key."""
    emitted: dict[str, Any] = {
        "scalar_type": _field(record, "scalar_type"),
        "value": _field(record, "value"),
    }
    return emitted


def serialize_string_literal(record: StringLiteral) -> dict[str, Any]:
    """One `StringLiteral` in contract key order: every required key, then each stated optional key."""
    emitted: dict[str, Any] = {
        "scalar_type": _field(record, "scalar_type"),
        "value": _field(record, "value"),
    }
    return emitted


def serialize_integer_literal(record: IntegerLiteral) -> dict[str, Any]:
    """One `IntegerLiteral` in contract key order: every required key, then each stated optional key."""
    emitted: dict[str, Any] = {
        "scalar_type": _field(record, "scalar_type"),
        "value": _field(record, "value"),
    }
    return emitted


def serialize_null_literal(record: NullLiteral) -> dict[str, Any]:
    """One `NullLiteral` in contract key order: every required key, then each stated optional key."""
    emitted: dict[str, Any] = {
        "scalar_type": _field(record, "scalar_type"),
    }
    return emitted


def serialize_program_authoring(record: ProgramAuthoring) -> dict[str, Any]:
    """One `ProgramAuthoring` branch, chosen by the `kind` the contract discriminates on."""
    discriminant = _field(record, "kind")
    if discriminant == PROGRAM_AUTHORING_KIND_WORKFLOW:
        return serialize_workflow_authoring(record)
    if discriminant == PROGRAM_AUTHORING_KIND_AGENT:
        return serialize_agent_authoring(record)
    raise ValueError(f"ProgramAuthoring states no branch for kind {discriminant!r}")


def serialize_value_expression(record: ValueExpression) -> dict[str, Any]:
    """One `ValueExpression` branch, chosen by the `kind` the contract discriminates on."""
    discriminant = _field(record, "kind")
    if discriminant == VALUE_EXPRESSION_KIND_SSA:
        return serialize_ssa_expression(record)
    if discriminant == VALUE_EXPRESSION_KIND_CONTEXT:
        return serialize_context_expression(record)
    if discriminant == VALUE_EXPRESSION_KIND_PROJECTION:
        return serialize_projection_expression(record)
    if discriminant == VALUE_EXPRESSION_KIND_OBJECT:
        return serialize_object_expression(record)
    if discriminant == VALUE_EXPRESSION_KIND_ARRAY:
        return serialize_array_expression(record)
    if discriminant == VALUE_EXPRESSION_KIND_STRING:
        return serialize_string_expression(record)
    if discriminant == VALUE_EXPRESSION_KIND_INTEGER:
        return serialize_integer_expression(record)
    if discriminant == VALUE_EXPRESSION_KIND_BOOLEAN:
        return serialize_boolean_expression(record)
    if discriminant == VALUE_EXPRESSION_KIND_NULL:
        return serialize_null_expression(record)
    raise ValueError(f"ValueExpression states no branch for kind {discriminant!r}")


def serialize_control_predicate(record: ControlPredicate) -> dict[str, Any]:
    """One `ControlPredicate` branch, chosen by the `comparator` the contract discriminates on."""
    discriminant = _field(record, "comparator")
    if discriminant == PREDICATE_COMPARATOR_TRUTHY:
        return serialize_truthy_predicate(record)
    if discriminant == PREDICATE_COMPARATOR_EQUALS:
        return serialize_equals_predicate(record)
    if discriminant == PREDICATE_COMPARATOR_NOT_EQUALS:
        return serialize_not_equals_predicate(record)
    raise ValueError(f"ControlPredicate states no branch for comparator {discriminant!r}")


def serialize_skill_instruction_source(record: SkillInstructionSource) -> dict[str, Any]:
    """One `SkillInstructionSource` branch, chosen by the `kind` the contract discriminates on."""
    discriminant = _field(record, "kind")
    if discriminant == SKILL_INSTRUCTION_KIND_ENTRY:
        return serialize_skill_entry_source(record)
    if discriminant == SKILL_INSTRUCTION_KIND_INLINE:
        return serialize_skill_inline_source(record)
    raise ValueError(f"SkillInstructionSource states no branch for kind {discriminant!r}")


def serialize_predicate_literal(record: PredicateLiteral) -> dict[str, Any]:
    """One `PredicateLiteral` branch, chosen by the `scalar_type` the contract discriminates on."""
    discriminant = _field(record, "scalar_type")
    if discriminant == PREDICATE_SCALAR_TYPE_BOOLEAN:
        return serialize_boolean_literal(record)
    if discriminant == PREDICATE_SCALAR_TYPE_STRING:
        return serialize_string_literal(record)
    if discriminant == PREDICATE_SCALAR_TYPE_INTEGER:
        return serialize_integer_literal(record)
    if discriminant == PREDICATE_SCALAR_TYPE_NULL:
        return serialize_null_literal(record)
    raise ValueError(f"PredicateLiteral states no branch for scalar_type {discriminant!r}")


__all__ = [
    "serialize_permission",
    "serialize_program_definition",
    "serialize_workflow_authoring",
    "serialize_agent_authoring",
    "serialize_entrypoint_input_schema",
    "serialize_event_requirement",
    "serialize_imported_program_ref",
    "serialize_declaration",
    "serialize_function_def",
    "serialize_parameter",
    "serialize_value",
    "serialize_ssa_expression",
    "serialize_context_expression",
    "serialize_projection_expression",
    "serialize_object_expression",
    "serialize_value_field",
    "serialize_array_expression",
    "serialize_string_expression",
    "serialize_integer_expression",
    "serialize_boolean_expression",
    "serialize_null_expression",
    "serialize_block",
    "serialize_region",
    "serialize_data_edge",
    "serialize_call_intent",
    "serialize_control_intent",
    "serialize_truthy_predicate",
    "serialize_equals_predicate",
    "serialize_not_equals_predicate",
    "serialize_context_edge",
    "serialize_hook_binding",
    "serialize_capability_requirement",
    "serialize_model_requirement",
    "serialize_skill_requirement",
    "serialize_skill_entry_source",
    "serialize_skill_inline_source",
    "serialize_boolean_literal",
    "serialize_string_literal",
    "serialize_integer_literal",
    "serialize_null_literal",
    "serialize_program_authoring",
    "serialize_value_expression",
    "serialize_control_predicate",
    "serialize_skill_instruction_source",
    "serialize_predicate_literal",
]
