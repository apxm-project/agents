// AUTO-GENERATED by apxm codegen frontend-vocabulary; DO NOT EDIT.
// The scopes a Hook declares itself over.
//
// A Hook states `scope: CAPABILITY` rather than `scope: "capability"`, so the
// scope an author writes is the scope the contract states rather than a string
// that has to match one. Every name here aliases the constant the internal
// source-graph vocabulary already carries, so each scope has one spelling.
//
// Authors reach these through `@apxm/frontend/scopes`, this module's public
// path.

import type { HookScope } from "./frontend-graph.js";
import {
  HOOK_SCOPE_AGENT,
  HOOK_SCOPE_LOOP,
  HOOK_SCOPE_NODE,
  HOOK_SCOPE_MODEL,
  HOOK_SCOPE_CAPABILITY,
  HOOK_SCOPES,
} from "./frontend-graph.js";

export type Scope = HookScope;
export const AGENT = HOOK_SCOPE_AGENT satisfies Scope;
export const LOOP = HOOK_SCOPE_LOOP satisfies Scope;
export const NODE = HOOK_SCOPE_NODE satisfies Scope;
export const MODEL = HOOK_SCOPE_MODEL satisfies Scope;
export const CAPABILITY = HOOK_SCOPE_CAPABILITY satisfies Scope;
export const SCOPES = HOOK_SCOPES satisfies readonly Scope[];
