[package]
name = "apxm-source-port"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true
publish = false

[lints]
workspace = true

[dependencies]
apxm-program.workspace = true
serde.workspace = true
serde_json.workspace = true

[dev-dependencies]
# Conformance pins the external-program fixture to the bytes its author
# committed. `apxm-program` digests artifacts but keeps its helper private, so
# the pin hashes for itself rather than widening that crate's surface for a test.
sha2.workspace = true
