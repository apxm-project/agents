# AUTO-GENERATED by apxm-frontend; DO NOT EDIT.
"""MLIR emission helpers for AIS operations."""

def emit_agent(**attrs):
    """Emit MLIR for AGENT operation."""
    parts = []

    parts.append("ais.agent")
    return ''.join(parts)

def emit_qmem(ctx=None, space=None, **attrs):
    """Emit MLIR for QMEM operation."""
    parts = []

    parts.append("%0 = ")
    parts.append("ais.qmem")
    if 'query' in attrs:
        parts.append(f' {attrs["query"]}"')
    if ctx:
        parts.append(" [")
        parts.append(", ".join(str(c) for c in ctx))
        parts.append("]")
    parts.append(" : !ais.token")
    return ''.join(parts)

def emit_umem(ctx=None, space=None, **attrs):
    """Emit MLIR for UMEM operation."""
    parts = []

    parts.append("%0 = ")
    parts.append("ais.umem")
    if 'key' in attrs:
        parts.append(f' {attrs["key"]}"')
    if ctx:
        parts.append(" (")
        parts.append(", ".join(str(c) for c in ctx))
        parts.append(")")
    parts.append(" : !ais.token")
    return ''.join(parts)

def emit_ask(ctx=None, temperature=None, model=None, **attrs):
    """Emit MLIR for ASK operation."""
    parts = []

    parts.append("%0 = ")
    parts.append("ais.ask")
    if 'template_str' in attrs:
        parts.append(f' {attrs["template_str"]}"')
    if ctx:
        parts.append(" [")
        parts.append(", ".join(str(c) for c in ctx))
        parts.append("]")
    parts.append(" : !ais.token")
    return ''.join(parts)

def emit_think(ctx=None, **attrs):
    """Emit MLIR for THINK operation."""
    parts = []

    parts.append("%0 = ")
    parts.append("ais.think")
    if ctx:
        parts.append(" [")
        parts.append(", ".join(str(c) for c in ctx))
        parts.append("]")
    parts.append(" : !ais.token")
    return ''.join(parts)

def emit_reason(ctx=None, **attrs):
    """Emit MLIR for REASON operation."""
    parts = []

    parts.append("%0 = ")
    parts.append("ais.reason")
    if ctx:
        parts.append(" [")
        parts.append(", ".join(str(c) for c in ctx))
        parts.append("]")
    parts.append(" : !ais.token")
    return ''.join(parts)

def emit_plan(ctx=None, **attrs):
    """Emit MLIR for PLAN operation."""
    parts = []

    parts.append("%0 = ")
    parts.append("ais.plan")
    if ctx:
        parts.append(" [")
        parts.append(", ".join(str(c) for c in ctx))
        parts.append("]")
    parts.append(" : !ais.token")
    return ''.join(parts)

def emit_reflect(ctx=None, **attrs):
    """Emit MLIR for REFLECT operation."""
    parts = []

    parts.append("%0 = ")
    parts.append("ais.reflect")
    if ctx:
        parts.append(" [")
        parts.append(", ".join(str(c) for c in ctx))
        parts.append("]")
    parts.append(" : !ais.token")
    return ''.join(parts)

def emit_verify(ctx=None, **attrs):
    """Emit MLIR for VERIFY operation."""
    parts = []

    parts.append("%0 = ")
    parts.append("ais.verify")
    if ctx:
        parts.append(" [")
        parts.append(", ".join(str(c) for c in ctx))
        parts.append("]")
    parts.append(" : !ais.token")
    return ''.join(parts)

def emit_inv_tool(ctx=None, **attrs):
    """Emit MLIR for INV_TOOL operation."""
    parts = []

    parts.append("%0 = ")
    parts.append("ais.inv_tool")
    if ctx:
        parts.append(" [")
        parts.append(", ".join(str(c) for c in ctx))
        parts.append("]")
    parts.append(" : !ais.token")
    return ''.join(parts)

def emit_exc(ctx=None, **attrs):
    """Emit MLIR for EXC operation."""
    parts = []

    parts.append("%0 = ")
    parts.append("ais.exc")
    if ctx:
        parts.append(" [")
        parts.append(", ".join(str(c) for c in ctx))
        parts.append("]")
    parts.append(" : !ais.token")
    return ''.join(parts)

def emit_print(ctx=None, **attrs):
    """Emit MLIR for PRINT operation."""
    parts = []

    parts.append("%0 = ")
    parts.append("ais.print")
    if ctx:
        parts.append(" [")
        parts.append(", ".join(str(c) for c in ctx))
        parts.append("]")
    parts.append(" : !ais.token")
    return ''.join(parts)

def emit_jump(ctx=None, **attrs):
    """Emit MLIR for JUMP operation."""
    parts = []

    parts.append("%0 = ")
    parts.append("ais.jump")
    if ctx:
        parts.append(" [")
        parts.append(", ".join(str(c) for c in ctx))
        parts.append("]")
    parts.append(" : !ais.token")
    return ''.join(parts)

def emit_branch_on_value(ctx=None, **attrs):
    """Emit MLIR for BRANCH_ON_VALUE operation."""
    parts = []

    parts.append("%0 = ")
    parts.append("ais.branch_on_value")
    if ctx:
        parts.append(" [")
        parts.append(", ".join(str(c) for c in ctx))
        parts.append("]")
    parts.append(" : !ais.token")
    return ''.join(parts)

def emit_loop_start(ctx=None, **attrs):
    """Emit MLIR for LOOP_START operation."""
    parts = []

    parts.append("%0 = ")
    parts.append("ais.loop_start")
    if ctx:
        parts.append(" [")
        parts.append(", ".join(str(c) for c in ctx))
        parts.append("]")
    parts.append(" : !ais.token")
    return ''.join(parts)

def emit_loop_end(ctx=None, **attrs):
    """Emit MLIR for LOOP_END operation."""
    parts = []

    parts.append("%0 = ")
    parts.append("ais.loop_end")
    if ctx:
        parts.append(" [")
        parts.append(", ".join(str(c) for c in ctx))
        parts.append("]")
    parts.append(" : !ais.token")
    return ''.join(parts)

def emit_return(ctx=None, **attrs):
    """Emit MLIR for RETURN operation."""
    parts = []

    parts.append("%0 = ")
    parts.append("ais.return")
    if ctx:
        parts.append(" [")
        parts.append(", ".join(str(c) for c in ctx))
        parts.append("]")
    parts.append(" : !ais.token")
    return ''.join(parts)

def emit_switch(ctx=None, **attrs):
    """Emit MLIR for SWITCH operation."""
    parts = []

    parts.append("%0 = ")
    parts.append("ais.switch")
    if ctx:
        parts.append(" [")
        parts.append(", ".join(str(c) for c in ctx))
        parts.append("]")
    parts.append(" : !ais.token")
    return ''.join(parts)

def emit_flow_call(ctx=None, **attrs):
    """Emit MLIR for FLOW_CALL operation."""
    parts = []

    parts.append("%0 = ")
    parts.append("ais.flow_call")
    if ctx:
        parts.append(" [")
        parts.append(", ".join(str(c) for c in ctx))
        parts.append("]")
    parts.append(" : !ais.token")
    return ''.join(parts)

def emit_merge(ctx=None, **attrs):
    """Emit MLIR for MERGE operation."""
    parts = []

    parts.append("%0 = ")
    parts.append("ais.merge")
    if ctx:
        parts.append(" [")
        parts.append(", ".join(str(c) for c in ctx))
        parts.append("]")
    parts.append(" : !ais.token")
    return ''.join(parts)

def emit_fence(ctx=None, **attrs):
    """Emit MLIR for FENCE operation."""
    parts = []

    parts.append("%0 = ")
    parts.append("ais.fence")
    if ctx:
        parts.append(" [")
        parts.append(", ".join(str(c) for c in ctx))
        parts.append("]")
    parts.append(" : !ais.token")
    return ''.join(parts)

def emit_wait_all(ctx=None, **attrs):
    """Emit MLIR for WAIT_ALL operation."""
    parts = []

    parts.append("%0 = ")
    parts.append("ais.wait_all")
    if ctx:
        parts.append(" [")
        parts.append(", ".join(str(c) for c in ctx))
        parts.append("]")
    parts.append(" : !ais.token")
    return ''.join(parts)

def emit_try_catch(ctx=None, **attrs):
    """Emit MLIR for TRY_CATCH operation."""
    parts = []

    parts.append("%0 = ")
    parts.append("ais.try_catch")
    if ctx:
        parts.append(" [")
        parts.append(", ".join(str(c) for c in ctx))
        parts.append("]")
    parts.append(" : !ais.token")
    return ''.join(parts)

def emit_err(ctx=None, **attrs):
    """Emit MLIR for ERR operation."""
    parts = []

    parts.append("%0 = ")
    parts.append("ais.err")
    if ctx:
        parts.append(" [")
        parts.append(", ".join(str(c) for c in ctx))
        parts.append("]")
    parts.append(" : !ais.token")
    return ''.join(parts)

def emit_communicate(ctx=None, **attrs):
    """Emit MLIR for COMMUNICATE operation."""
    parts = []

    parts.append("%0 = ")
    parts.append("ais.communicate")
    if ctx:
        parts.append(" [")
        parts.append(", ".join(str(c) for c in ctx))
        parts.append("]")
    parts.append(" : !ais.token")
    return ''.join(parts)

def emit_update_goal(ctx=None, **attrs):
    """Emit MLIR for UPDATE_GOAL operation."""
    parts = []

    parts.append("%0 = ")
    parts.append("ais.update_goal")
    if ctx:
        parts.append(" [")
        parts.append(", ".join(str(c) for c in ctx))
        parts.append("]")
    parts.append(" : !ais.token")
    return ''.join(parts)

def emit_guard(ctx=None, **attrs):
    """Emit MLIR for GUARD operation."""
    parts = []

    parts.append("%0 = ")
    parts.append("ais.guard")
    if ctx:
        parts.append(" [")
        parts.append(", ".join(str(c) for c in ctx))
        parts.append("]")
    parts.append(" : !ais.token")
    return ''.join(parts)

def emit_claim(ctx=None, **attrs):
    """Emit MLIR for CLAIM operation."""
    parts = []

    parts.append("%0 = ")
    parts.append("ais.claim")
    if ctx:
        parts.append(" [")
        parts.append(", ".join(str(c) for c in ctx))
        parts.append("]")
    parts.append(" : !ais.token")
    return ''.join(parts)

def emit_pause(ctx=None, **attrs):
    """Emit MLIR for PAUSE operation."""
    parts = []

    parts.append("%0 = ")
    parts.append("ais.pause")
    if ctx:
        parts.append(" [")
        parts.append(", ".join(str(c) for c in ctx))
        parts.append("]")
    parts.append(" : !ais.token")
    return ''.join(parts)

def emit_resume(ctx=None, **attrs):
    """Emit MLIR for RESUME operation."""
    parts = []

    parts.append("%0 = ")
    parts.append("ais.resume")
    if ctx:
        parts.append(" [")
        parts.append(", ".join(str(c) for c in ctx))
        parts.append("]")
    parts.append(" : !ais.token")
    return ''.join(parts)

def emit_delegate(ctx=None, **attrs):
    """Emit MLIR for DELEGATE operation."""
    parts = []

    parts.append("%0 = ")
    parts.append("ais.delegate")
    if ctx:
        parts.append(" [")
        parts.append(", ".join(str(c) for c in ctx))
        parts.append("]")
    parts.append(" : !ais.token")
    return ''.join(parts)

def emit_negotiate(ctx=None, **attrs):
    """Emit MLIR for NEGOTIATE operation."""
    parts = []

    parts.append("%0 = ")
    parts.append("ais.negotiate")
    if ctx:
        parts.append(" [")
        parts.append(", ".join(str(c) for c in ctx))
        parts.append("]")
    parts.append(" : !ais.token")
    return ''.join(parts)

def emit_nop(ctx=None, **attrs):
    """Emit MLIR for NOP operation."""
    parts = []

    parts.append("%0 = ")
    parts.append("ais.nop")
    if ctx:
        parts.append(" [")
        parts.append(", ".join(str(c) for c in ctx))
        parts.append("]")
    parts.append(" : !ais.token")
    return ''.join(parts)

def emit_identity(ctx=None, **attrs):
    """Emit MLIR for IDENTITY operation."""
    parts = []

    parts.append("%0 = ")
    parts.append("ais.identity")
    if ctx:
        parts.append(" [")
        parts.append(", ".join(str(c) for c in ctx))
        parts.append("]")
    parts.append(" : !ais.token")
    return ''.join(parts)

def emit_spawn_agent(ctx=None, **attrs):
    """Emit MLIR for SPAWN_AGENT operation."""
    parts = []

    parts.append("%0 = ")
    parts.append("ais.spawn_agent")
    if ctx:
        parts.append(" [")
        parts.append(", ".join(str(c) for c in ctx))
        parts.append("]")
    parts.append(" : !ais.token")
    return ''.join(parts)

def emit_spawn_team(ctx=None, **attrs):
    """Emit MLIR for SPAWN_TEAM operation."""
    parts = []

    parts.append("%0 = ")
    parts.append("ais.spawn_team")
    if ctx:
        parts.append(" [")
        parts.append(", ".join(str(c) for c in ctx))
        parts.append("]")
    parts.append(" : !ais.token")
    return ''.join(parts)

def emit_register_capability(ctx=None, **attrs):
    """Emit MLIR for REGISTER_CAPABILITY operation."""
    parts = []

    parts.append("%0 = ")
    parts.append("ais.register_capability")
    if ctx:
        parts.append(" [")
        parts.append(", ".join(str(c) for c in ctx))
        parts.append("]")
    parts.append(" : !ais.token")
    return ''.join(parts)

def emit_autonomous(ctx=None, **attrs):
    """Emit MLIR for AUTONOMOUS operation."""
    parts = []

    parts.append("%0 = ")
    parts.append("ais.autonomous")
    if ctx:
        parts.append(" [")
        parts.append(", ".join(str(c) for c in ctx))
        parts.append("]")
    parts.append(" : !ais.token")
    return ''.join(parts)

def emit_checkpoint(ctx=None, **attrs):
    """Emit MLIR for CHECKPOINT operation."""
    parts = []

    parts.append("%0 = ")
    parts.append("ais.checkpoint")
    if ctx:
        parts.append(" [")
        parts.append(", ".join(str(c) for c in ctx))
        parts.append("]")
    parts.append(" : !ais.token")
    return ''.join(parts)

def emit_const_str(ctx=None, **attrs):
    """Emit MLIR for CONST_STR operation."""
    parts = []

    parts.append("%0 = ")
    parts.append("ais.const_str")
    if ctx:
        parts.append(" [")
        parts.append(", ".join(str(c) for c in ctx))
        parts.append("]")
    parts.append(" : !ais.token")
    return ''.join(parts)

def emit_yield(ctx=None, **attrs):
    """Emit MLIR for YIELD operation."""
    parts = []

    parts.append("%0 = ")
    parts.append("ais.yield")
    if ctx:
        parts.append(" [")
        parts.append(", ".join(str(c) for c in ctx))
        parts.append("]")
    parts.append(" : !ais.token")
    return ''.join(parts)

