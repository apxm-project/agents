# AUTO-GENERATED by apxm-frontend; DO NOT EDIT.
_CODEGEN_HASH: str = "e2f74334d7773951"
"""MLIR emission helpers for AIS operations."""
from collections.abc import Callable
from typing import Any, Final

def _quote(s: str) -> str:
    escaped = s.replace('\\', '\\\\').replace('"', '\\"').replace('\n', '\\n').replace('\t', '\\t').replace('\r', '\\r')
    return f'"{escaped}"'


def emit_agent(ssa_name: str, attrs: dict[str, Any], inputs: list[str]) -> str:
    """Emit MLIR for AGENT operation."""
    ctx = ""
    primary = ""
    kw_str = ""
    return f"ais.agent{primary}{ctx}{kw_str}"


def emit_qmem(ssa_name: str, attrs: dict[str, Any], inputs: list[str]) -> str:
    """Emit MLIR for QMEM operation."""
    ctx = f" [{', '.join(inputs)} : {', '.join(['!ais.token'] * len(inputs))}]" if inputs else ""
    primary = f' {_quote(str(attrs["query"]))}' if "query" in attrs else ""
    kw_parts = []
    if "memory_tier" in attrs and attrs["memory_tier"] is not None:
        kw_parts.append(f'memory_tier={attrs["memory_tier"]}')
    kw_str = f' {{{", ".join(kw_parts)}}}' if kw_parts else ""
    return f"{ssa_name} = ais.qmem{primary}{ctx}{kw_str} : !ais.token"


def emit_umem(ssa_name: str, attrs: dict[str, Any], inputs: list[str]) -> str:
    """Emit MLIR for UMEM operation."""
    ctx = f" ({', '.join(inputs)} : {', '.join(['!ais.token'] * len(inputs))})" if inputs else ""
    primary = f' {_quote(str(attrs["key"]))}' if "key" in attrs else ""
    kw_parts = []
    if "memory_tier" in attrs and attrs["memory_tier"] is not None:
        kw_parts.append(f'memory_tier={attrs["memory_tier"]}')
    kw_str = f' {{{", ".join(kw_parts)}}}' if kw_parts else ""
    return f"{ssa_name} = ais.umem{primary}{ctx}{kw_str} : !ais.token"


def emit_ask(ssa_name: str, attrs: dict[str, Any], inputs: list[str]) -> str:
    """Emit MLIR for ASK operation."""
    ctx = f" [{', '.join(inputs)} : {', '.join(['!ais.token'] * len(inputs))}]" if inputs else ""
    primary = f' {_quote(str(attrs["template_str"]))}' if "template_str" in attrs else ""
    kw_parts = []
    if "temperature" in attrs and attrs["temperature"] is not None:
        kw_parts.append(f'temperature={attrs["temperature"]}')
    if "model" in attrs and attrs["model"] is not None:
        kw_parts.append(f'model={attrs["model"]}')
    kw_str = f' {{{", ".join(kw_parts)}}}' if kw_parts else ""
    return f"{ssa_name} = ais.ask{primary}{ctx}{kw_str} : !ais.token"


def emit_think(ssa_name: str, attrs: dict[str, Any], inputs: list[str]) -> str:
    """Emit MLIR for THINK operation."""
    ctx = f" [{', '.join(inputs)} : {', '.join(['!ais.token'] * len(inputs))}]" if inputs else ""
    primary = ""
    kw_str = ""
    return f"{ssa_name} = ais.think{primary}{ctx}{kw_str} : !ais.token"


def emit_reason(ssa_name: str, attrs: dict[str, Any], inputs: list[str]) -> str:
    """Emit MLIR for REASON operation."""
    ctx = f" [{', '.join(inputs)} : {', '.join(['!ais.token'] * len(inputs))}]" if inputs else ""
    primary = ""
    kw_str = ""
    return f"{ssa_name} = ais.reason{primary}{ctx}{kw_str} : !ais.token"


def emit_plan(ssa_name: str, attrs: dict[str, Any], inputs: list[str]) -> str:
    """Emit MLIR for PLAN operation."""
    ctx = f" [{', '.join(inputs)} : {', '.join(['!ais.token'] * len(inputs))}]" if inputs else ""
    primary = ""
    kw_str = ""
    return f"{ssa_name} = ais.plan{primary}{ctx}{kw_str} : !ais.token"


def emit_reflect(ssa_name: str, attrs: dict[str, Any], inputs: list[str]) -> str:
    """Emit MLIR for REFLECT operation."""
    ctx = f" [{', '.join(inputs)} : {', '.join(['!ais.token'] * len(inputs))}]" if inputs else ""
    primary = ""
    kw_str = ""
    return f"{ssa_name} = ais.reflect{primary}{ctx}{kw_str} : !ais.token"


def emit_verify(ssa_name: str, attrs: dict[str, Any], inputs: list[str]) -> str:
    """Emit MLIR for VERIFY operation."""
    ctx = f" [{', '.join(inputs)} : {', '.join(['!ais.token'] * len(inputs))}]" if inputs else ""
    primary = ""
    kw_str = ""
    return f"{ssa_name} = ais.verify{primary}{ctx}{kw_str} : !ais.token"


def emit_inv_tool(ssa_name: str, attrs: dict[str, Any], inputs: list[str]) -> str:
    """Emit MLIR for INV_TOOL operation."""
    ctx = f" [{', '.join(inputs)} : {', '.join(['!ais.token'] * len(inputs))}]" if inputs else ""
    primary = ""
    kw_str = ""
    return f"{ssa_name} = ais.inv_tool{primary}{ctx}{kw_str} : !ais.token"


def emit_exc(ssa_name: str, attrs: dict[str, Any], inputs: list[str]) -> str:
    """Emit MLIR for EXC operation."""
    ctx = f" [{', '.join(inputs)} : {', '.join(['!ais.token'] * len(inputs))}]" if inputs else ""
    primary = ""
    kw_str = ""
    return f"{ssa_name} = ais.exc{primary}{ctx}{kw_str} : !ais.token"


def emit_print(ssa_name: str, attrs: dict[str, Any], inputs: list[str]) -> str:
    """Emit MLIR for PRINT operation."""
    ctx = f" [{', '.join(inputs)} : {', '.join(['!ais.token'] * len(inputs))}]" if inputs else ""
    primary = ""
    kw_str = ""
    return f"{ssa_name} = ais.print{primary}{ctx}{kw_str} : !ais.token"


def emit_jump(ssa_name: str, attrs: dict[str, Any], inputs: list[str]) -> str:
    """Emit MLIR for JUMP operation."""
    ctx = f" [{', '.join(inputs)} : {', '.join(['!ais.token'] * len(inputs))}]" if inputs else ""
    primary = ""
    kw_str = ""
    return f"{ssa_name} = ais.jump{primary}{ctx}{kw_str} : !ais.token"


def emit_branch_on_value(ssa_name: str, attrs: dict[str, Any], inputs: list[str]) -> str:
    """Emit MLIR for BRANCH_ON_VALUE operation."""
    ctx = f" [{', '.join(inputs)} : {', '.join(['!ais.token'] * len(inputs))}]" if inputs else ""
    primary = ""
    kw_str = ""
    return f"{ssa_name} = ais.branch_on_value{primary}{ctx}{kw_str} : !ais.token"


def emit_loop_start(ssa_name: str, attrs: dict[str, Any], inputs: list[str]) -> str:
    """Emit MLIR for LOOP_START operation."""
    ctx = f" [{', '.join(inputs)} : {', '.join(['!ais.token'] * len(inputs))}]" if inputs else ""
    primary = ""
    kw_str = ""
    return f"{ssa_name} = ais.loop_start{primary}{ctx}{kw_str} : !ais.token"


def emit_loop_end(ssa_name: str, attrs: dict[str, Any], inputs: list[str]) -> str:
    """Emit MLIR for LOOP_END operation."""
    ctx = f" [{', '.join(inputs)} : {', '.join(['!ais.token'] * len(inputs))}]" if inputs else ""
    primary = ""
    kw_str = ""
    return f"{ssa_name} = ais.loop_end{primary}{ctx}{kw_str} : !ais.token"


def emit_return(ssa_name: str, attrs: dict[str, Any], inputs: list[str]) -> str:
    """Emit MLIR for RETURN operation."""
    ctx = f" [{', '.join(inputs)} : {', '.join(['!ais.token'] * len(inputs))}]" if inputs else ""
    primary = ""
    kw_str = ""
    return f"{ssa_name} = ais.return{primary}{ctx}{kw_str} : !ais.token"


def emit_switch(ssa_name: str, attrs: dict[str, Any], inputs: list[str]) -> str:
    """Emit MLIR for SWITCH operation."""
    ctx = f" [{', '.join(inputs)} : {', '.join(['!ais.token'] * len(inputs))}]" if inputs else ""
    primary = ""
    kw_str = ""
    return f"{ssa_name} = ais.switch{primary}{ctx}{kw_str} : !ais.token"


def emit_flow_call(ssa_name: str, attrs: dict[str, Any], inputs: list[str]) -> str:
    """Emit MLIR for FLOW_CALL operation."""
    ctx = f" [{', '.join(inputs)} : {', '.join(['!ais.token'] * len(inputs))}]" if inputs else ""
    primary = ""
    kw_str = ""
    return f"{ssa_name} = ais.flow_call{primary}{ctx}{kw_str} : !ais.token"


def emit_merge(ssa_name: str, attrs: dict[str, Any], inputs: list[str]) -> str:
    """Emit MLIR for MERGE operation."""
    ctx = f" [{', '.join(inputs)} : {', '.join(['!ais.token'] * len(inputs))}]" if inputs else ""
    primary = ""
    kw_str = ""
    return f"{ssa_name} = ais.merge{primary}{ctx}{kw_str} : !ais.token"


def emit_fence(ssa_name: str, attrs: dict[str, Any], inputs: list[str]) -> str:
    """Emit MLIR for FENCE operation."""
    ctx = f" [{', '.join(inputs)} : {', '.join(['!ais.token'] * len(inputs))}]" if inputs else ""
    primary = ""
    kw_str = ""
    return f"{ssa_name} = ais.fence{primary}{ctx}{kw_str} : !ais.token"


def emit_wait_all(ssa_name: str, attrs: dict[str, Any], inputs: list[str]) -> str:
    """Emit MLIR for WAIT_ALL operation."""
    ctx = f" [{', '.join(inputs)} : {', '.join(['!ais.token'] * len(inputs))}]" if inputs else ""
    primary = ""
    kw_str = ""
    return f"{ssa_name} = ais.wait_all{primary}{ctx}{kw_str} : !ais.token"


def emit_try_catch(ssa_name: str, attrs: dict[str, Any], inputs: list[str]) -> str:
    """Emit MLIR for TRY_CATCH operation."""
    ctx = f" [{', '.join(inputs)} : {', '.join(['!ais.token'] * len(inputs))}]" if inputs else ""
    primary = ""
    kw_str = ""
    return f"{ssa_name} = ais.try_catch{primary}{ctx}{kw_str} : !ais.token"


def emit_err(ssa_name: str, attrs: dict[str, Any], inputs: list[str]) -> str:
    """Emit MLIR for ERR operation."""
    ctx = f" [{', '.join(inputs)} : {', '.join(['!ais.token'] * len(inputs))}]" if inputs else ""
    primary = ""
    kw_str = ""
    return f"{ssa_name} = ais.err{primary}{ctx}{kw_str} : !ais.token"


def emit_communicate(ssa_name: str, attrs: dict[str, Any], inputs: list[str]) -> str:
    """Emit MLIR for COMMUNICATE operation."""
    ctx = f" [{', '.join(inputs)} : {', '.join(['!ais.token'] * len(inputs))}]" if inputs else ""
    primary = ""
    kw_str = ""
    return f"{ssa_name} = ais.communicate{primary}{ctx}{kw_str} : !ais.token"


def emit_update_goal(ssa_name: str, attrs: dict[str, Any], inputs: list[str]) -> str:
    """Emit MLIR for UPDATE_GOAL operation."""
    ctx = f" [{', '.join(inputs)} : {', '.join(['!ais.token'] * len(inputs))}]" if inputs else ""
    primary = ""
    kw_str = ""
    return f"{ssa_name} = ais.update_goal{primary}{ctx}{kw_str} : !ais.token"


def emit_guard(ssa_name: str, attrs: dict[str, Any], inputs: list[str]) -> str:
    """Emit MLIR for GUARD operation."""
    ctx = f" [{', '.join(inputs)} : {', '.join(['!ais.token'] * len(inputs))}]" if inputs else ""
    primary = ""
    kw_str = ""
    return f"{ssa_name} = ais.guard{primary}{ctx}{kw_str} : !ais.token"


def emit_claim(ssa_name: str, attrs: dict[str, Any], inputs: list[str]) -> str:
    """Emit MLIR for CLAIM operation."""
    ctx = f" [{', '.join(inputs)} : {', '.join(['!ais.token'] * len(inputs))}]" if inputs else ""
    primary = ""
    kw_str = ""
    return f"{ssa_name} = ais.claim{primary}{ctx}{kw_str} : !ais.token"


def emit_pause(ssa_name: str, attrs: dict[str, Any], inputs: list[str]) -> str:
    """Emit MLIR for PAUSE operation."""
    ctx = f" [{', '.join(inputs)} : {', '.join(['!ais.token'] * len(inputs))}]" if inputs else ""
    primary = ""
    kw_str = ""
    return f"{ssa_name} = ais.pause{primary}{ctx}{kw_str} : !ais.token"


def emit_resume(ssa_name: str, attrs: dict[str, Any], inputs: list[str]) -> str:
    """Emit MLIR for RESUME operation."""
    ctx = f" [{', '.join(inputs)} : {', '.join(['!ais.token'] * len(inputs))}]" if inputs else ""
    primary = ""
    kw_str = ""
    return f"{ssa_name} = ais.resume{primary}{ctx}{kw_str} : !ais.token"


def emit_delegate(ssa_name: str, attrs: dict[str, Any], inputs: list[str]) -> str:
    """Emit MLIR for DELEGATE operation."""
    ctx = f" [{', '.join(inputs)} : {', '.join(['!ais.token'] * len(inputs))}]" if inputs else ""
    primary = ""
    kw_str = ""
    return f"{ssa_name} = ais.delegate{primary}{ctx}{kw_str} : !ais.token"


def emit_negotiate(ssa_name: str, attrs: dict[str, Any], inputs: list[str]) -> str:
    """Emit MLIR for NEGOTIATE operation."""
    ctx = f" [{', '.join(inputs)} : {', '.join(['!ais.token'] * len(inputs))}]" if inputs else ""
    primary = ""
    kw_str = ""
    return f"{ssa_name} = ais.negotiate{primary}{ctx}{kw_str} : !ais.token"


def emit_nop(ssa_name: str, attrs: dict[str, Any], inputs: list[str]) -> str:
    """Emit MLIR for NOP operation."""
    ctx = f" [{', '.join(inputs)} : {', '.join(['!ais.token'] * len(inputs))}]" if inputs else ""
    primary = ""
    kw_str = ""
    return f"{ssa_name} = ais.nop{primary}{ctx}{kw_str} : !ais.token"


def emit_identity(ssa_name: str, attrs: dict[str, Any], inputs: list[str]) -> str:
    """Emit MLIR for IDENTITY operation."""
    ctx = f" [{', '.join(inputs)} : {', '.join(['!ais.token'] * len(inputs))}]" if inputs else ""
    primary = ""
    kw_str = ""
    return f"{ssa_name} = ais.identity{primary}{ctx}{kw_str} : !ais.token"


def emit_spawn_agent(ssa_name: str, attrs: dict[str, Any], inputs: list[str]) -> str:
    """Emit MLIR for SPAWN_AGENT operation."""
    ctx = f" [{', '.join(inputs)} : {', '.join(['!ais.token'] * len(inputs))}]" if inputs else ""
    primary = ""
    kw_str = ""
    return f"{ssa_name} = ais.spawn_agent{primary}{ctx}{kw_str} : !ais.token"


def emit_spawn_team(ssa_name: str, attrs: dict[str, Any], inputs: list[str]) -> str:
    """Emit MLIR for SPAWN_TEAM operation."""
    ctx = f" [{', '.join(inputs)} : {', '.join(['!ais.token'] * len(inputs))}]" if inputs else ""
    primary = ""
    kw_str = ""
    return f"{ssa_name} = ais.spawn_team{primary}{ctx}{kw_str} : !ais.token"


def emit_register_capability(ssa_name: str, attrs: dict[str, Any], inputs: list[str]) -> str:
    """Emit MLIR for REGISTER_CAPABILITY operation."""
    ctx = f" [{', '.join(inputs)} : {', '.join(['!ais.token'] * len(inputs))}]" if inputs else ""
    primary = ""
    kw_str = ""
    return f"{ssa_name} = ais.register_capability{primary}{ctx}{kw_str} : !ais.token"


def emit_autonomous(ssa_name: str, attrs: dict[str, Any], inputs: list[str]) -> str:
    """Emit MLIR for AUTONOMOUS operation."""
    ctx = f" [{', '.join(inputs)} : {', '.join(['!ais.token'] * len(inputs))}]" if inputs else ""
    primary = ""
    kw_str = ""
    return f"{ssa_name} = ais.autonomous{primary}{ctx}{kw_str} : !ais.token"


def emit_checkpoint(ssa_name: str, attrs: dict[str, Any], inputs: list[str]) -> str:
    """Emit MLIR for CHECKPOINT operation."""
    ctx = f" [{', '.join(inputs)} : {', '.join(['!ais.token'] * len(inputs))}]" if inputs else ""
    primary = ""
    kw_str = ""
    return f"{ssa_name} = ais.checkpoint{primary}{ctx}{kw_str} : !ais.token"


def emit_const_str(ssa_name: str, attrs: dict[str, Any], inputs: list[str]) -> str:
    """Emit MLIR for CONST_STR operation."""
    ctx = f" [{', '.join(inputs)} : {', '.join(['!ais.token'] * len(inputs))}]" if inputs else ""
    primary = ""
    kw_str = ""
    return f"{ssa_name} = ais.const_str{primary}{ctx}{kw_str} : !ais.token"


def emit_yield(ssa_name: str, attrs: dict[str, Any], inputs: list[str]) -> str:
    """Emit MLIR for YIELD operation."""
    ctx = f" [{', '.join(inputs)} : {', '.join(['!ais.token'] * len(inputs))}]" if inputs else ""
    primary = ""
    kw_str = ""
    return f"{ssa_name} = ais.yield{primary}{ctx}{kw_str} : !ais.token"


EMITTERS: dict[str, Callable] = {
    "AGENT": emit_agent,
    "QMEM": emit_qmem,
    "UMEM": emit_umem,
    "ASK": emit_ask,
    "THINK": emit_think,
    "REASON": emit_reason,
    "PLAN": emit_plan,
    "REFLECT": emit_reflect,
    "VERIFY": emit_verify,
    "INV_TOOL": emit_inv_tool,
    "EXC": emit_exc,
    "PRINT": emit_print,
    "JUMP": emit_jump,
    "BRANCH_ON_VALUE": emit_branch_on_value,
    "LOOP_START": emit_loop_start,
    "LOOP_END": emit_loop_end,
    "RETURN": emit_return,
    "SWITCH": emit_switch,
    "FLOW_CALL": emit_flow_call,
    "MERGE": emit_merge,
    "FENCE": emit_fence,
    "WAIT_ALL": emit_wait_all,
    "TRY_CATCH": emit_try_catch,
    "ERR": emit_err,
    "COMMUNICATE": emit_communicate,
    "UPDATE_GOAL": emit_update_goal,
    "GUARD": emit_guard,
    "CLAIM": emit_claim,
    "PAUSE": emit_pause,
    "RESUME": emit_resume,
    "DELEGATE": emit_delegate,
    "NEGOTIATE": emit_negotiate,
    "NOP": emit_nop,
    "IDENTITY": emit_identity,
    "SPAWN_AGENT": emit_spawn_agent,
    "SPAWN_TEAM": emit_spawn_team,
    "REGISTER_CAPABILITY": emit_register_capability,
    "AUTONOMOUS": emit_autonomous,
    "CHECKPOINT": emit_checkpoint,
    "CONST_STR": emit_const_str,
    "YIELD": emit_yield,
}

VOID_OPS: Final[frozenset] = frozenset({"AGENT", "JUMP", "BRANCH_ON_VALUE", "LOOP_START", "LOOP_END", "FENCE"})

TEMPLATE_ATTRS: Final[frozenset] = frozenset({"template_str", "prompt", "template", "value"})
