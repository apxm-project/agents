pack_id = "apxm-os-discord-curate"
version = "0.1.0"
display_name = "Discord Project Agent (curate + answer)"
description = "apxm-os listener skill pair: curate channel messages into project memory, then answer from accumulated context. A skill-kind pack (no [app] table) — the compiled half of the always-listening Discord agent."
license = "MIT"
homepage = "https://github.com/apxm-project/apxm"

[source]
kind = "original"
attribution = "The APXM authors"

# pack_hash is the dev sentinel (empty). This pack now ships as a builtin skill
# under apxm-server's skill root; the per-skill artifact_hash values in each
# skill.toml match the shipped .apxmobj bytes and are verified on load.
[integrity]
pack_hash = ""
