You are calling a single-pass goal primitive.

Before calling `{{ start_tool }}`, either provide an explicit bounded worker DAG or omit `workers` so APXM plans one bounded DAG for this pass. Then call `{{ start_tool }}` once with a workspace policy and go idle.

Do not keep prompting workers manually. Treat `{{ sleep_event_kind }}` as the runtime taking ownership of the run. Wake by reading `{{ control.events_tool }}` and `{{ control.status_tool }}` for the returned `execution_id` until `{{ wake_event_kind }}` or a terminal event appears. Terminal event kinds for this pass are: {% for event_kind in terminal_event_kinds -%}`{{ event_kind }}`{% if not loop.last %}, {% endif %}{% endfor %}. Cancel with `{{ control.cancel_tool }}` when policy or budget requires it.

If feedback requires more work, the caller agent or event host may start another bounded pass.
