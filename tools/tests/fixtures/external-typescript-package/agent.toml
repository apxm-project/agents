id = "external-typescript-fixture"
version = "0.1.0"
schema_version = "apxm.agent"
display_name = "External TypeScript Fixture"
description = "A public-package conformance fixture for the canonical TypeScript frontend."
domain = "testing"
kind = "agent"
license = "MIT"

[compile]
entry = "src/main.ts"
frontend = "typescript"
