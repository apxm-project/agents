# Agent notes

This repository owns the APXM compiler and runtime source used by CLIC's pinned cohort.

- Run the repository's `dekk check` and focused owner tests for protocol changes.
- Preserve the published Unix JSONL contracts, release manifest, and source attestation.
- Keep compiler and runtime implementation details behind their owner protocols.
- Never place CLIC Company data or credentials in this repository.
