# 0002 gate evidence — chat thin clients

**Date:** 2026-06-17  
**Repo:** `apxm`  
**Scope:** Remaining 0002 checkpoints and T101 full gate.

## Commands

| Command | Result | Evidence |
|---|---:|---|
| `cargo check -p apxm-server` | PASS | Server crate checks; warnings only. |
| `cargo test -p apxm-runtime session_ledger --lib` | PASS | 8 session ledger tests passed. |
| `cargo test -p apxm-server --test contract_errors` | PASS | 5 typed-error contract tests passed. |
| `cargo test -p apxm-server --test contract_session_api` | PASS | 9 session API tests passed, including `sc003_turn_cap_denies_conversation_message_without_client_counting`. |
| `cargo test -p apxm-server --test permission_roundtrip` | PASS | 1 permission round-trip test passed. |
| `specs/0002-apxm-chat-thin-clients/scripts/check_cli_no_ledger.sh` | PASS | CLI has no host-side ledger counters. |
| `dekk apxm test-cli` | PASS | 15 CLI tests passed. |
| `dekk apxm test` | PASS | Full apxm invariant suite passed, including server contract tests, SSE backpressure, OpenAPI diff, runtime session ledger, and doctests. |

## Audit notes

- A first attempt using `dekk apxm test -p apxm-server contract_errors` exited 0 but ran zero integration tests because the filter did not select the test file. The proof above uses Cargo's explicit `--test` targets and the full `dekk apxm test` suite.
- `cargo fmt --check` is not a valid completion gate for this spec in the current worktree: it reports broad pre-existing formatting drift outside the touched files. Touched Rust files were formatted directly with `rustfmt --edition 2024`.
- The full gate passed with warnings only; no failing tests remain for 0002.
