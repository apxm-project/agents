# T072 quickstart evidence — 2026-06-16

UTC timestamp: `2026-06-16T06:11:45Z`
Latest update: `2026-06-16T06:26:30Z`
Workspace: `/home/raherrer/projects/apxm-project/apxm`
Feature: `specs/0001-agent-in-program`

## Evidence target

`quickstart.md` defines the two reference hosts:

- Host A: interactive terminal via `apxm chat`.
- Host B: hosted service via `POST /v1/execute/stream` plus
  `POST /v1/conversations/{session_id}/message`.

T072 remains **partial**. The first pass completed fresh Host B evidence for
SC-001, SC-004, SC-005, and SC-007; fresh Host A evidence for SC-001 plus one
SC-005 selection; and found a fresh SC-006 blocker. A follow-up code fix and
focused rerun resolved the Host B direct SC-006 duplicate-registration blocker.
SC-003 was not rerun in this pass.

## Setup and prerequisites

- `dekk apxm doctor`:
  - MLIR toolchain ready.
  - APXM compiler context initialization succeeded.
  - One backend configured.
  - Warning only: `APXM_BACKEND` not set.
- `PYTHONPATH=crates/compiler/apxm-frontend/python python3 examples/python/conversational/controllable_agent.py --validate`:
  - Output: `VALID`.
- `dekk apxm execute examples/python/conversational/controllable_agent.py --emit-air > .apxm/t072-controllable-agent.air`:
  - Failed as a pure emitter in this checkout; it attempted to execute the
    in-graph recv entry and returned:
    `AUTONOMOUS - recv mode requires a recv_url attribute ... unless the execution has a session id`.
  - Workaround for live evidence: direct fixture AIR emission with
    `PYTHONPATH=crates/compiler/apxm-frontend/python python3 examples/python/conversational/controllable_agent.py > .apxm/t072-controllable-agent.air`.
- `dekk apxm analyze .apxm/t072-controllable-agent.air`:
  - Failed to parse the comment-prefixed AIR artifact:
    `error[E900]: module parsing: operation returned null`.
- Direct SC-005 preflight against the stale server on `127.0.0.1:18800` returned
  0/10 because the representative skill catalogue was not loaded and that
  process returned 404 for `/v1/capabilities/rescan`.
- Local five-pack SC-005 fixtures were installed under the active user skill
  root `~/.apxm/libs/{support,engineering,security,docs,analysis}` and the
  running isolated server then listed
  `data_viz`, `release_notes`, `architecture_review`, `security_scan`,
  `ticket_triage`, `webhook_ack`, and `prompt-as-workflow`.
- Direct `/v1/capabilities/search_skills/invoke` preflight on
  `127.0.0.1:18920` returned 10/10 correct top-1 results.

## Server provenance

- `dekk apxm server --port 18920`:
  - Started current isolated server, but raw Python-backed AIR was rejected
    without trust/sandbox env:
    `raw execute does not support python-backed tool handlers`.
- `APXM_TRUST_PYTHON_ARTIFACTS=1 APXM_SANDBOX_PYTHON=1 dekk apxm server --port 18921`:
  - Accepted Python-backed AIR but exposed a stale release-binary blocker:
    `Failed to parse worker response: unknown variant host_call`.
- `dekk apxm build-server`:
  - Completed successfully.
  - Warning observed: pre-existing dead-code warning for
    `start_skill_execution_with_provenance`.
- `APXM_TRUST_PYTHON_ARTIFACTS=1 APXM_SANDBOX_PYTHON=1 dekk apxm server --port 18922`:
  - Fresh rebuilt trusted/sandboxed server used for the passing Host A/Host B
    evidence below.

## SC-006 duplicate-registration fix rerun

Root cause: artifact flow DAGs were registered into the runtime-wide
`FlowRegistry`, so a long-lived server could carry `researcher.main` from a
previous artifact into a later `chat_agent.py` run. `SPAWN_AGENT researcher`
then rejected the stale registered flow before writing current-run agent info.

Fix verification:

- `dekk apxm test -p apxm-runtime artifact_flow_registry_is_execution_scoped`:
  PASS. Regression covers executing an artifact that declares `researcher.main`
  and then a second artifact that runs `SPAWN_AGENT researcher` on the same
  runtime.
- `dekk apxm build-server`: PASS, with the pre-existing
  `start_skill_execution_with_provenance` dead-code warning.
- `PYTHONPATH=crates/compiler/apxm-frontend/python python3 examples/python/conversational/chat_agent.py > .apxm/t072-chat-agent-fixed.air`:
  emitted the focused SC-006 AIR.
- `APXM_TRUST_PYTHON_ARTIFACTS=1 APXM_SANDBOX_PYTHON=1 dekk apxm server --port 18924`:
  fresh rebuilt trusted/sandboxed server for the fixed rerun.

Focused Host B SC-006 rerun:

- Endpoint: `http://127.0.0.1:18924`
- Session: `t072-host-b-sc006-fixed-direct-20260616T0626`
- Raw log:
  `.apxm/evaluation/t072-quickstart-20260616T0556/host-b-sc006-delegate-fixed-direct-18924.log`
- Result: PASS for the prior duplicate-registration blocker. The stream
  completed with `failed_nodes=0`, `executed_nodes=10`, `SAW_TOKEN True`,
  `SAW_ERROR False`, and no `Agent 'researcher' already exists` error. The
  `execute_complete` payload included node `4` with `name: researcher`,
  a `process_id`, and the inline research `system_prompt`, and node `10`
  included the final synthesized reply.
- Operator error during setup, not counted as evidence:
  `.apxm/evaluation/t072-quickstart-20260616T0556/host-b-sc006-delegate-fixed-18924.log`
  used the session turn-input shape against single-turn `chat_agent.py` and
  correctly failed argument validation (`conversation` arg missing).

## Host B — hosted service

Endpoint: `http://127.0.0.1:18922`

Raw logs:

- `.apxm/evaluation/t072-quickstart-20260616T0556/host-b-smoke-rebuilt-trusted.log`
- `.apxm/evaluation/t072-quickstart-20260616T0556/host-b-basic-sweep.log`
- `.apxm/evaluation/t072-quickstart-20260616T0556/host-b-sc005-conversational.log`
- `.apxm/evaluation/t072-quickstart-20260616T0556/host-b-sc006-delegate.log`

Results:

- SC-001: PASS. Session `t072-host-b-basic-20260616T0608` registered and woke
  every turn. Turn 1 returned `OK`; tool turn returned
  `The exact lookup result for the ticker AMD is: [price of AMD].`; recall turn
  returned `BLUEHERON`.
- SC-004: PASS for block/edit evidence on Host B. The AMD request proved the
  pre-tool edit to uppercase; the `FORBIDDEN` request returned a blocked-tool
  explanation from the pre-tool hook.
- SC-005: PASS on Host B through the conversational-agent path. Session
  `t072-host-b-sc005-20260616T0610` returned 10/10 expected top skill IDs:
  `ticket_triage`, `architecture_review`, `security_scan`, `release_notes`,
  `data_viz`, `architecture_review`, `ticket_triage`, `security_scan`,
  `release_notes`, `data_viz`.
- SC-007: PASS smoke on Host B. After prior turns, an additional message in
  session `t072-host-b-basic-20260616T0608` returned `STILL_RUNNING` over the
  same long-lived execution (`execution_id` unchanged in turn-input responses).
- SC-003: NOT RERUN in this pass.
- SC-006: PASS on the fixed direct Host B rerun above. The original fresh
  blocker on `127.0.0.1:18922` is preserved in
  `.apxm/evaluation/t072-quickstart-20260616T0556/host-b-sc006-delegate.log`:
  `SPAWN_AGENT - Agent 'researcher' already exists in the flow registry`.

## Host A — interactive terminal

Command:

```bash
{ printf 'Remember that the project codename is BLUEHERON. Reply exactly OK.\n'; sleep 18; printf 'Use the lookup tool for ticker amd, then include the exact lookup result.\n'; sleep 28; printf 'What project codename did I give you in the first turn? Reply with only the codename.\n'; sleep 22; printf 'Use the search_skills tool for this exact request: review module boundaries and frontend composability. Use the visible imports support, engineering, docs, analysis. Reply with only the top skill_id and no explanation.\n'; sleep 28; printf '/exit\n'; } | dekk apxm chat --air .apxm/t072-controllable-agent.air --server http://127.0.0.1:18922 --session-id t072-host-a-cli-20260616T0614 --import support --import engineering --import docs --import analysis
```

Raw log:

- `.apxm/evaluation/t072-quickstart-20260616T0556/host-a-cli-basic-sc005.log`

Results:

- SC-001: PASS smoke on Host A. Output included `OK`, the AMD lookup result, and
  `BLUEHERON` recall.
- SC-005: PASS smoke on Host A for one representative request. The terminal host
  returned `architecture_review`.
- SC-003, SC-004 full hook matrix, SC-006, and full 10-request SC-005 on Host A:
  NOT RERUN in this pass.

## Current status

T072 remains partial. Fresh passing evidence exists for the main Host B
conversational-agent path including the previously missing SC-005 campaign, for
Host B direct SC-006 after the duplicate-registration fix, and for Host A basic
parity plus one SC-005 selection. Remaining fresh evidence: SC-003 on both
hosts, full Host A SC-004/SC-005, and Host A SC-006.
