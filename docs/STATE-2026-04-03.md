# APXM Project State — April 3, 2026

_Last updated: 2026-04-03 by APXM agent_

---

## What Is APXM

APXM is a Rust-based agentic AI runtime with a 4-stage MLIR compiler, a graph-aware execution engine, and a multi-backend LLM dispatch system. It compiles declarative agent workflow graphs (`.json`) into optimized execution artifacts, then runs them with parallel scheduling, memoization, circuit-breaker routing, and ACP-agent orchestration.

**17 crates**, ~400 tests passing.

---

## Infrastructure

| Node | Role | Hardware |
|------|------|----------|
| `useocpm2m-097-083` | Build / dev / APXM runtime | CPU only |
| `useocpm2m-097-045` | Inference | 8× accelerator hardware GPU (192GB HBM3 each = **1.5TB GPU RAM**) |

**Shared storage**: `/shared_inference/models/` (NFS, accessible from both nodes)

**vLLM running now** on 097-045:
- Container: `vllm-gemma3-v2`
- Image: `gpu/pytorch-private:vllm-v0.14.0_amd_dev_aiter_nixl_ravgupta`
- Model: `Gemma-3-27B-it` at `/shared_inference/models/Google/Gemma-3-27b-it`
- API: `http://localhost:8000` (OpenAI-compatible)
- TP: 4× GPU, bfloat16, max 8192 tokens
- **Status: ✅ live and responding**

---

## Execution Plan Status

### Phase 1: ModelRouter ✅ COMPLETE
_Weeks 1–3 — Dynamic LLM backend/model selection with circuit breakers_

| Deliverable | Status | Commit |
|-------------|--------|--------|
| `ModelRouter` struct + 6-step routing priority | ✅ | `a2da3b8` |
| Circuit breaker (Closed→Open→HalfOpen, configurable) | ✅ | `a2da3b8` |
| Model registry loading `~/.apxm/models.toml` | ✅ | `a2da3b8` |
| ModelRouter wired into `ExecutionContext` | ✅ | `a2da3b8` |
| ModelRouter initialized in driver after LLM backends register | ✅ | `a5aa3b4` |
| `apxm models list` CLI command | ✅ | `a5aa3b4` |
| `apxm models health` CLI command | ✅ | `a5aa3b4` |

All 400+ tests pass. Zero breaking changes.

---

### Phase 2: AcpCapability ✅ COMPLETE (pre-existing)
_Weeks 4–6 — ACP agent orchestration from within APXM graphs_

| Deliverable | Status | Location |
|-------------|--------|----------|
| `AcpCapability` (INV with capability="acp") | ✅ | `apxm-acp/src/capability.rs` |
| `AcpSession` (subprocess + JSON-RPC 2.0 over stdio) | ✅ | `apxm-acp/src/session.rs` |
| `AgentRegistry` (`~/.apxm/agents.toml`) | ✅ | `apxm-acp/src/registry.rs` |
| `SessionPool` (multi-turn session reuse) | ✅ | `apxm-acp/src/session_pool.rs` |
| `ReverseHandler` (file I/O, terminal ops from agent) | ✅ | `apxm-acp/src/reverse.rs` |
| Auto-registered in driver capabilities | ✅ | `apxm-driver/src/runtime/capabilities.rs` |
| `apxm agent list/add/remove/test` CLI | ✅ | `apxm-cli/src/main.rs` |
| 16 agent profiles (claude, codex, gemini, …) | ✅ | builtin |

---

### Phase 3: Graph-Aware vLLM ✅ COMPLETE (core)
_Weeks 7–11 — vLLM becomes aware of APXM execution graphs_

#### Rust side (apxm-backends) — commit `7c77b62`
| Deliverable | Status |
|-------------|--------|
| `ProviderProtocol::Vllm` variant + builtin provider | ✅ |
| `GraphAwareVllmBackend` (wraps OpenAI backend, injects hints) | ✅ |
| `ApxmGraphHints` struct (graph_id, node_id, priority_class, pin_policy, downstream_nodes) | ✅ |
| `GraphMetadata` + `NodeSpec` for graph registration | ✅ |
| `PinPolicy` (prefix/none + TTL) | ✅ |
| `CompilerHints` (shared_prefix_est_tokens, warmup_candidate) | ✅ |
| `LLMRequest::apxm_hints` + `with_apxm_hints()` builder | ✅ |
| `register_graph()` → `POST /v1/apxm/graphs/register` | ✅ |
| `release_graph()` → `DELETE /v1/apxm/graphs/{id}` | ✅ |
| `ProviderProtocol::Vllm` in credentials validator | ✅ |

#### Python side (vllm `apxm` branch) — commit `73e130d`
| Deliverable | Status |
|-------------|--------|
| `POST /v1/apxm/graphs/register` endpoint + Pydantic schemas | ✅ |
| `GET /v1/apxm/graphs/{graph_id}` status endpoint | ✅ |
| `DELETE /v1/apxm/graphs/{graph_id}` release endpoint | ✅ |
| `apxm_graph_registry` in scheduler (stores graph metadata) | ✅ |
| Critical-path priority scheduling (`priority = -1`) | ✅ |
| Graph-level `default_pin_ttl_ms` for KV-cache pins | ✅ |
| Full engine chain: scheduler → EngineCore → core_client → AsyncLLM → DPLB | ✅ |
| `ApxmRequestHints` parsed from `extra_body.apxm` per request | ✅ (pre-existing) |
| KV-cache pinning with TTL + memory pressure eviction | ✅ (pre-existing) |

#### Validated end-to-end today ✅
```
APXM graph (.json)
  → dekk compiler (4-stage MLIR)
  → APXM runtime (parallel scheduler)
  → ASK handler → gemma3-local backend
  → GraphAwareVllmBackend (http://localhost:8000)
  → vLLM 0.14.0 + GPU runtime 7.0 + aiter kernels
  → 4× accelerator hardware GPU
  → Gemma-3-27B-it response
  → back through runtime → printed output
```

Ran parallel 2-node graph (WAIT_ALL + MERGE + synthesis) successfully.

#### Phase 3 remaining (Week 11 tasks)
| Task | Status |
|------|--------|
| Integration tests (APXM graph → vLLM hints roundtrip) | ⏳ pending |
| Criterion benchmarks (with/without graph hints) | ⏳ pending |
| `ContextStack` foundation (`apxm-runtime/src/context_stack/`) | ⏳ pending |

These should go through `./launch-feature.sh` on 097-083.

---

### Phase 4: Patent + Optimization Targets
_Weeks 12–16 — NOT STARTED_

| Task | Status | Target |
|------|--------|--------|
| `ContextStack` (spaghetti context propagation) | ⏳ | `apxm-runtime/src/context_stack/` |
| Context assembly (bottom-up/top-down traversal) | ⏳ | same |
| Demand paging (lazy context loading) | ⏳ | same |
| `MemoCache` (two-tier: DashMap + SQLite) | ⏳ | `apxm-runtime/src/memo/` |
| Speculative execution (commit/rollback) | ⏳ | same |
| Token pipelining (stream tokens into next node's prefill) | ⏳ | executor + vllm backend |
| `OptimizationTarget` enum + `--target` CLI flag | ⏳ | `apxm-compiler/src/passes/targets.rs` |
| `ModelDowngrade` MLIR pass | ⏳ | `apxm-compiler/mlir/` |
| `ContextBudget` MLIR pass | ⏳ | same |
| `ParallelismExtraction` MLIR pass | ⏳ | same |
| `SpeculationInsertion` MLIR pass | ⏳ | same |
| `PipelineInsertion` MLIR pass | ⏳ | same |
| Patent documentation | ⏳ | `docs/` |
| Benchmarks (latency, throughput, cost efficiency) | ⏳ | `benches/` |

---

## Feature Workflow

**Rule: all new features go through the workflow, never hand-coded.**

```bash
# On 097-083
cd ~/projects/agents/apxm
./launch-feature.sh "your feature description"
```

This runs:
1. 3 parallel THINK agents (architect + implementation + test-focused)
2. MERGE + synthesis into one unified plan
3. Claude Code implements, builds, fixes, commits

---

## Git State

| Repo | Branch | Latest Commit | Node |
|------|--------|---------------|------|
| `apxm` | `main` | `7c77b62` feat(backends): Phase 3 Rust | 097-083 |
| `vllm` fork | `apxm` | `73e130d` feat(apxm): Phase 3 vLLM extensions | 097-045 |

---

## Immediate Next Steps

1. **Phase 3 Week 11** — run `launch-feature.sh` for ContextStack + benchmarks + integration tests
2. **Gemma 4** — get HF token, accept license at `huggingface.co/google/gemma-4-31B-it`, pull to `/shared_inference/models/`, restart Docker with `gemma-4-31B-it`
3. **Phase 4 start** — begin ContextStack via workflow (it's the foundation for everything in Phase 4)
4. **All 8 GPUs** — restart vLLM with `--tensor-parallel-size 8` for full throughput
5. **Gemma-3 as workflow backend** — replace the default LLM backend in `launch-feature.sh` with `gemma3-local` so the APXM workflow runs on GPU

---

## Key Files

| File | Purpose |
|------|---------|
| `docs/EXECUTION-PLAN.md` | Full 16-week plan with all tasks |
| `examples/14-add-feature/add-apxm-feature.json` | The feature workflow graph |
| `launch-feature.sh` | Wrapper to run the workflow |
| `~/.apxm/credentials.toml` | LLM backend credentials (corp-gateway, gemma3-local) |
| `~/.apxm/models.toml` | Model routing config (optional) |
| `~/.apxm/agents.toml` | ACP agent profiles |
| `/shared_inference/models/` | Model weights (on 097-045) |
