allowed_agent_skills = []
capabilities = [
    "edit",
    "read",
    "test",
]
description = "A read-only coding extension that reads source and prepares edit and test proposals."
display_name = "Coder"
domain = "software"
id = "coder"
kind = "agent"
license = "MIT"
schema_version = "apxm.agent"
version = "0.1.0"

[compile]
entry = "src/main.ts"
frontend = "typescript"

[prompts]
persona = "prompts/persona.md"
safety = "prompts/safety.md"

[source]
type = "local"
