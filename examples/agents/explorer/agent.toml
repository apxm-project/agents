capabilities = ["read"]
description = "A read-only explorer that reports workspace findings to its parent."
display_name = "Explorer"
domain = "software"
id = "explorer"
kind = "agent"
license = "MIT"
schema_version = "apxm.agent.v1"
skills = []
version = "0.1.0"

[prompts]
persona = "prompts/persona.md"

[runtime]
memory_space = "stm"
session_prefix = "explorer"

[runtime.loop]
mode = "host"
rearm = true
turn_param = "user_message"

[source]
type = "local"
