capabilities = [
    "read",
    "search_web",
    "write",
]
display_name = "Conversational (No Memory)"
domain = "conversational-opt-out"
id = "conversational-opt-out"
kind = "agent"
license = "MIT"
schema_version = "apxm.agent.v1"
skills = []
version = "0.1.0"

[chat]
capability_discovery = true

[prompts]
persona = "prompts/persona.md"

[runtime]
session_prefix = "conversational-opt-out"

[runtime.loop]
mode = "host"
rearm = true
turn_param = "user_message"

[source]
type = "local"
