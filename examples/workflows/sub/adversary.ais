// adversary.ais - converted from adversary.apxm
//
// Usage: dekk apxm execute examples/workflows/sub/adversary.apxm

agent Adversary {
    @entry flow main(TASK: str) -> str {
        // TODO: REASON node 1 (adversary_reason)
        return ""
    }
}
