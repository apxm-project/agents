module {
  func.func @trigger(%arg0: !ais.token {ais.param_name = "event", ais.param_type = "str"}, %arg1: !ais.token {ais.param_name = "policy", ais.param_type = "str"}) -> !ais.token attributes {ais.entry} {
    %label = ais.const_str "trigger.policy_gate:" : !ais.token
    %event = ais.merge %label, %arg0 : !ais.token, !ais.token -> !ais.token
    %out = ais.merge %event, %arg1 : !ais.token, !ais.token -> !ais.token
    func.return %out : !ais.token
  }
}
