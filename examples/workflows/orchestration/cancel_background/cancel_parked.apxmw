{
  "name": "cancel_parked",
  "description": "Park on a checkpoint so apxm_workflow_cancel can interrupt the workflow.",
  "graphs": [
    {
      "id": "wait_for_cancel",
      "path": "wait_for_cancel.air"
    }
  ],
  "output": "{{wait_for_cancel.output}}"
}
