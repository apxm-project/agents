{
  "name": "approval_gate",
  "description": "Park on a checkpoint, wake from the APXM checkpoint endpoint, and return the resumed input.",
  "graphs": [
    {
      "id": "wait_for_approval",
      "path": "approval_gate.air"
    }
  ],
  "output": "approval: {{wait_for_approval.output}}"
}
