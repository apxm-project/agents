module {
  func.func @trigger(%arg0: !ais.token {ais.param_name = "event", ais.param_type = "str"}) -> !ais.token attributes {ais.entry} {
    %label = ais.const_str "trigger:" : !ais.token
    %out = ais.merge %label, %arg0 : !ais.token, !ais.token -> !ais.token
    func.return %out : !ais.token
  }
}
