{
  "name": "background_ok",
  "description": "Fast deterministic workflow suited for workflow run --background smoke tests.",
  "steps": [
    {
      "id": "ok",
      "path": "ok.air"
    }
  ],
  "output": "{{ok.output}}"
}
