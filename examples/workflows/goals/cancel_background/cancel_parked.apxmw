{
  "name": "cancel_parked",
  "description": "Park on a checkpoint so workflow_cancel can interrupt the workflow.",
  "steps": [
    {
      "id": "wait_for_cancel",
      "path": "wait_for_cancel.air"
    }
  ],
  "output": "{{wait_for_cancel.output}}"
}
