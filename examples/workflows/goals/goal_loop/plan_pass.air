module {
  func.func @plan_pass(%arg0: !ais.token {ais.param_name = "goal", ais.param_type = "str"}, %arg1: !ais.token {ais.param_name = "trigger", ais.param_type = "str"}) -> !ais.token attributes {ais.entry} {
    %label = ais.const_str "bounded.pass_request:" : !ais.token
    %goal = ais.merge %label, %arg0 : !ais.token, !ais.token -> !ais.token
    %out = ais.merge %goal, %arg1 : !ais.token, !ais.token -> !ais.token
    func.return %out : !ais.token
  }
}
