# Implementation blocked

No implementation can be performed yet because the concrete task definition is missing: `{{TASK}}` is still a placeholder.

## Required next input
Please provide:

```text
Task:
<real task here>

Optional extra context:
- relevant modules/files
- expected API shape
- performance requirements
- failure model / reliability expectations
- whether this touches tokio, rayon, shared state, channels, cancellation, or persistence
```

## Constraints
Until the task is provided, do not:
- invent APIs
- add new crates/dependencies
- change Cargo manifests
- create placeholder handlers/services/types
- guess repository structure
- implement tests against assumed behavior
