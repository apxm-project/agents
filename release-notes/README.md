# Release Notes

Each APXM release has one notes file named `vX.Y.Z.md`. The release tooling
uses that file for `dekk agents release publish`.

Keep notes focused on user-visible changes, release artifacts, and upgrade
steps. Detailed engineering history belongs in `CHANGELOG.md`.
